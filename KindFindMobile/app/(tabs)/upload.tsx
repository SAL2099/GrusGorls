import React from "react"; // Import React to use JSX syntax
import UploadScreen from "../../components/uploadScreen"; // Import the UploadScreen component, which contains the UI and logic for uploading items

export default function ProfileUploadScreen() {
  return <UploadScreen />; // Render the UploadScreen component when this screen is accessed
}
